# Turso 数据库（留空则使用本地 file:./data/local.db）
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# Cloudflare Workers AI（M2 摘要用，暂不需要）
CF_ACCOUNT_ID=
CF_AI_API_TOKEN=
# 摘要模型（可选，默认 @cf/meta/llama-3.1-8b-instruct）
CF_AI_MODEL=

# 本地开发代理（Node fetch 不读系统代理；GH Actions 上无需设置，留空即可）
HTTPS_PROXY=
